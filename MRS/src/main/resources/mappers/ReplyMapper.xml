<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.music.mrs.user.mapper.IReplyMapper">

<insert id="replyInsert">

	INSERT INTO reply
		(user_no, reply_content, regDate)
	VALUES
		(#{userNo}, #{replyContent}, #{regDate})

</insert>

<select id="replyList" resultType="reply">

	SELECT 
		r.replyContent , u.display_name , r.regDate
	FROM reply r, user u
	WHERE r.user_no = {user_no} = u.user_no = {user_no}

</select>

<update id="update">

	UPDATE reply
	SET replyContent = #{replyContent}
	WHERE replyNo = #{replyNo}

</update>

<delete id="delete">

	DELETE FROM reply
	WHERE replyNo =  #{replyNO}

</delete>
</mapper>